# Codex local configuration

`config.toml` is intentionally local and ignored by Git because MCP configuration can contain credentials.

Run `npm run banani:prepare`, then paste the Banani MCP configuration directly into `.codex/config.toml`. Never commit or share its token.
